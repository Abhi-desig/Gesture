{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The page needs exactly the window controls behind pill mode — WKWebView freezes frame delivery to any non-visible page, so when the window would be hidden while the camera runs, the page shrinks it to a tiny always-on-top pill instead. Everything else still goes over HTTP to the Node server, so nothing else is granted.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:4321", "http://localhost:4321"]
  },
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "core:window:allow-set-position",
    "core:window:allow-set-always-on-top",
    "core:window:allow-set-visible-on-all-workspaces",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-inner-position",
    "core:window:allow-inner-size",
    "core:window:allow-outer-position",
    "core:window:allow-outer-size",
    "core:window:allow-current-monitor"
  ]
}
