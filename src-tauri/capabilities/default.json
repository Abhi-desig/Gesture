{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Nothing is granted, because this process renders nothing. It has no windows and no webview — the UI runs in Chrome and talks to the Node server over HTTP. The tray, start-at-login and server lifecycle are driven from Rust, where capabilities do not apply.",
  "windows": [],
  "permissions": []
}
